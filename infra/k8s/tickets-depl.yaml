# Define a Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tickets-depl # Name of the Deployment
spec:
  replicas: 1 # Number of pod replicas to run
  selector:
    matchLabels:
      app: tickets # Select pods with the label 'app: tickets'
  template:
    metadata:
      labels:
        app: tickets # Apply the label 'app: tickets' to the pods created by this template
    spec:
      containers:
        - name: tickets # Name of the container
          image: comauro7511/tickets # Docker image to use for this container
          env:
            - name: MONGO_URI # Environment variable to set in the container
              value: 'mongodb://tickets-mongo-srv:27017/tickets'
            - name: JWT_KEY # Environment variable to set in the container
              valueFrom:
                secretKeyRef:
                  name: jwt-secret # Reference to the Kubernetes Secret named 'jwt-secret'
                  key: JWT_KEY # The key in the Secret to use for this environment variable

---
# Define a Service
apiVersion: v1
kind: Service
metadata:
  name: tickets-srv # Name of the Service
spec:
  type: ClusterIP # Service type; ClusterIP exposes the Service on a cluster-internal IP
  selector:
    app: tickets # Select pods with the label 'app: tickets'
  ports:
    - name: tickets # Name of the port
      protocol: TCP # Protocol to use
      port: 3000 # Port on which the Service is exposed
      targetPort: 3000 # Port on the pod that the Service should forward traffic to
